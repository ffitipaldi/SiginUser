@if (ExibirAlerta)
{
    <div class="alert alert-secondary mt-4" role="alertdialog">
        @ChildContent
    </div>
}

@code {
    [Parameter]
    public bool ExibirAlerta { get; set; }

    [Parameter]
    public RenderFragment ChildContent { get; set; }
}
